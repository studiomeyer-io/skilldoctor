# Example Project

This is an AGENTS.md file — plain markdown, no frontmatter required.

## Build and test

- Install: `npm install`
- Test: `npm test`

## Code style

Use TypeScript strict mode and avoid `any`.
