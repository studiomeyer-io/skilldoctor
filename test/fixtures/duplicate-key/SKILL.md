---
name: duplicate-key
description: A skill whose frontmatter declares the same key twice so YAML keeps only the last. Use when testing duplicate-key detection.
name: duplicate-key-override
---

# Duplicate key

Body content.
